Add tsconfig.json to root
Add build task: ctrl+shift+p, config task runner
        typescript
        this will create tasks.json under .vscode
To compile: ctrl+shift+B

in command line it is:
    tsc xxx.ts

